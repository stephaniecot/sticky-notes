import {
    Link
  } from "react-router-dom";

const Navigation = () => {
    return (
        <nav>
        <Link to="/addPost">Ajouter un post</Link>
        </nav>
    )
}

export default Navigation